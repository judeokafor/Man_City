import React from 'react';
import Layout from './hoc/Layout';

const Routes = props => {
   return (
      <div className='App'>
         <Layout>Second line</Layout>
      </div>
   );
};

export default Routes;
